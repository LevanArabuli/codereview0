---
phase: 03-prompt-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/prompt.ts
  - tests/prompt.test.ts
autonomous: true
requirements:
  - PROMPT-01
  - PROMPT-03

must_haves:
  truths:
    - "Balanced mode prompt contains concrete anti-examples with code snippets showing what NOT to flag"
    - "All severity levels (bug, security, suggestion, nitpick) have few-shot anchoring examples in the prompt"
    - "Severity examples are identical in quick mode and agentic mode prompts (shared constant)"
    - "Anti-examples appear only in balanced mode, not in strict/detailed/lenient"
    - "All existing tests pass with no regression"
  artifacts:
    - path: "src/prompt.ts"
      provides: "SEVERITY_EXAMPLES shared constant and expanded MODE_OVERLAYS.balanced"
      contains: "SEVERITY_EXAMPLES"
    - path: "tests/prompt.test.ts"
      provides: "Test assertions for anti-examples and severity examples"
      contains: "anti-example"
  key_links:
    - from: "src/prompt.ts (SEVERITY_EXAMPLES)"
      to: "buildPrompt()"
      via: "string interpolation in basePrompt template"
      pattern: "SEVERITY_EXAMPLES"
    - from: "src/prompt.ts (SEVERITY_EXAMPLES)"
      to: "buildAgenticPrompt()"
      via: "string interpolation in basePrompt template"
      pattern: "SEVERITY_EXAMPLES"
    - from: "src/prompt.ts (MODE_OVERLAYS.balanced)"
      to: "getModeOverlay()"
      via: "Record lookup"
      pattern: "This is NOT a finding"
---

<objective>
Add severity anchoring examples and balanced-mode anti-examples to prompt templates

Purpose: Anchor the model's severity labels to concrete code examples and suppress low-value findings (TypeScript compiler-catchable issues, style noise) in balanced mode through explicit anti-examples with code snippets.

Output: Updated `src/prompt.ts` with a new `SEVERITY_EXAMPLES` shared constant and expanded `MODE_OVERLAYS.balanced`, plus test assertions in `tests/prompt.test.ts` validating the new content.
</objective>

<execution_context>
@/Users/levanarabuli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/levanarabuli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-prompt-foundations/03-CONTEXT.md
@.planning/phases/03-prompt-foundations/03-RESEARCH.md

<interfaces>
<!-- Key types and constants the executor needs from src/prompt.ts -->

From src/prompt.ts (lines 28-45) -- MODE_OVERLAYS structure:
```typescript
const MODE_OVERLAYS: Record<ReviewMode, string> = {
  strict: `\n\nREVIEW MODE — STRICT: ...`,
  detailed: `\n\nREVIEW MODE — DETAILED: ...`,
  lenient: `\n\nREVIEW MODE — LENIENT: ...`,
  balanced: `\n\nREVIEW MODE — BALANCED: ...
Do NOT report:
- File formatting issues (trailing newlines, whitespace, indentation)
- Idiomatic language/framework patterns ...
- Theoretical concerns without evidence ...

Before including a suggestion, ask: "Would a senior engineer ..."

Focus on being a helpful colleague: flag what matters, skip what doesn't.`,
};
```

From src/prompt.ts (lines 51-64) -- shared constant pattern to follow:
```typescript
const FINDING_FORMAT_INSTRUCTIONS = `For each issue found, provide:
- file: the file path exactly as shown in the diff
- line: ...
- severity: one of "bug", "security", "suggestion", or "nitpick"
...`;
```

From src/prompt.ts (lines 122-130) -- buildPrompt insertion area:
```typescript
${FINDING_FORMAT_INSTRUCTIONS}
// <-- SEVERITY_EXAMPLES goes here (after FINDING_FORMAT_INSTRUCTIONS)
Focus on the CHANGED code...
```

From src/prompt.ts (lines 230-242) -- buildAgenticPrompt insertion area:
```typescript
${FINDING_FORMAT_INSTRUCTIONS}
// <-- SEVERITY_EXAMPLES goes here (after FINDING_FORMAT_INSTRUCTIONS)
Report all issues you find...
```

From src/prompt.ts (line 92-94) -- getModeOverlay export:
```typescript
export function getModeOverlay(mode: ReviewMode): string {
  return MODE_OVERLAYS[mode];
}
```

From src/schemas.ts -- ReviewFindingSchema field names for examples:
Fields: file, line, severity, confidence, category, description
Optional: endLine, suggestedFix, relatedLocations
Severity enum: "bug" | "security" | "suggestion" | "nitpick"
Confidence enum: "high" | "medium" | "low"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SEVERITY_EXAMPLES constant and expand balanced mode anti-examples</name>
  <files>src/prompt.ts, tests/prompt.test.ts</files>
  <behavior>
    - Test: balanced overlay contains "This is NOT a finding" framing text
    - Test: balanced overlay mentions TypeScript compiler patterns (TS error codes or "TypeScript" keyword)
    - Test: anti-examples do NOT appear in strict, detailed, or lenient overlays
    - Test: buildPrompt output contains severity anchoring examples with all 4 severity levels ("severity": "bug", "severity": "security", "severity": "suggestion", "severity": "nitpick")
    - Test: buildAgenticPrompt output contains severity anchoring examples with all 4 severity levels
    - Test: severity examples text is identical in quick and agentic prompts (shared constant verification)
    - Test: all existing prompt.test.ts tests continue to pass
  </behavior>
  <action>
**Write tests first** in `tests/prompt.test.ts` (RED phase):

Add a new `describe('anti-examples in balanced mode')` block with tests:
1. `it('balanced overlay contains concrete anti-example snippets')` -- assert `getModeOverlay('balanced')` contains `'This is NOT a finding'`
2. `it('balanced overlay anti-examples mention TypeScript compiler')` -- assert overlay matches `/typescript|TS error|ts\d{4}/i`
3. `it('anti-examples only in balanced mode')` -- assert `getModeOverlay('strict')`, `getModeOverlay('detailed')`, `getModeOverlay('lenient')` do NOT contain `'This is NOT a finding'`

Add a new `describe('severity anchoring examples')` block with tests:
4. `it('buildPrompt includes severity anchoring examples')` -- build a prompt with mock PR data and assert it matches `/"severity":\s*"bug"/` and similar for all 4 levels
5. `it('buildAgenticPrompt includes severity anchoring examples')` -- same assertions against agentic prompt
6. `it('severity examples identical in quick and agentic prompts')` -- extract the SEVERITY_EXAMPLES substring from both prompts (search for a unique marker like `'correctly labeled findings'`) and assert they are equal

Use existing mock PR data patterns already in prompt.test.ts. Import `getModeOverlay`, `buildPrompt`, `buildAgenticPrompt` from `../src/prompt.js`.

Run tests to confirm RED (failures expected for new assertions).

**Then implement in `src/prompt.ts`** (GREEN phase):

1. Create a new `SEVERITY_EXAMPLES` constant at module level (after `FINDING_FORMAT_INSTRUCTIONS`, before `JSON_RESPONSE_INSTRUCTION`). Content:
   - Opening line: `"Here are examples of correctly labeled findings at each severity level:"`
   - 4 examples, each with: severity label header, 2-4 lines of language-agnostic code in a fenced code block, followed by a compact JSON object showing the expected finding
   - **bug**: Array access without bounds check (e.g., `users[0].name` on possibly-empty query result)
   - **security**: SQL injection via string concatenation
   - **suggestion**: Imperative loop replaceable with filter/map
   - **nitpick**: Unused import
   - Keep total under 60 lines. Use plain code format (no diff markers).

2. Insert `${SEVERITY_EXAMPLES}` into `buildPrompt()` basePrompt template -- place it on a new line after `${FINDING_FORMAT_INSTRUCTIONS}` and before the "Focus on the CHANGED code" paragraph (around line 123).

3. Insert `${SEVERITY_EXAMPLES}` into `buildAgenticPrompt()` basePrompt template -- place it on a new line after `${FINDING_FORMAT_INSTRUCTIONS}` and before the "Report all issues you find" paragraph (around line 232).

4. Expand `MODE_OVERLAYS.balanced` -- insert anti-example content between the existing "Do NOT report:" bullet list and the "Before including a suggestion" paragraph. Add 3-4 concrete anti-examples with code snippets using "This is NOT a finding" framing:
   - TypeScript unused variable (TS6133)
   - Missing return type on private method
   - Implicit any caught by strict mode (TS7006)
   - Trailing newline at end of file (style noise)
   Use plain code blocks (triple backticks). Do NOT modify or remove any existing text in the balanced overlay.

Run tests to confirm GREEN (all new + existing tests pass).
  </action>
  <verify>
    <automated>cd /Users/levanarabuli/development/codereview0 && npx vitest run tests/prompt.test.ts</automated>
  </verify>
  <done>
    - MODE_OVERLAYS.balanced contains 3-4 anti-examples with "This is NOT a finding" framing and TypeScript-focused code snippets
    - SEVERITY_EXAMPLES constant exists with 4 examples (bug, security, suggestion, nitpick), each with code + expected JSON
    - Both buildPrompt() and buildAgenticPrompt() include SEVERITY_EXAMPLES in their output
    - Anti-examples appear only in balanced mode overlay
    - All prompt.test.ts tests pass (existing + new)
  </done>
</task>

<task type="auto">
  <name>Task 2: Full test suite regression check and type validation</name>
  <files>src/prompt.ts</files>
  <action>
Run the full test suite (`npm test`) and type checker (`npm run lint`) to verify no regressions. The eval test suite (`tests/eval.test.ts`) exercises the prompt against fixture PRs and will detect if prompt changes cause structural issues.

If any test fails:
- For prompt.test.ts failures: fix the prompt content in src/prompt.ts (likely a string-matching assertion that needs the existing text preserved)
- For eval.test.ts failures: investigate whether the prompt change caused a structural issue (malformed JSON, missing fields) vs. a content quality change (different findings). Structural issues must be fixed. Content quality changes are expected and acceptable.
- For type errors: fix TypeScript issues in src/prompt.ts

If all tests pass, no changes needed -- this task confirms the green state.
  </action>
  <verify>
    <automated>cd /Users/levanarabuli/development/codereview0 && npm test && npm run lint</automated>
  </verify>
  <done>
    - `npm test` passes (all 223+ tests including eval.test.ts fixture suite)
    - `npm run lint` passes (TypeScript strict mode, no type errors)
    - No regressions introduced by prompt content changes
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run tests/prompt.test.ts` -- all prompt tests pass including new anti-example and severity example assertions
2. `npm test` -- full suite passes with no regressions
3. `npm run lint` -- TypeScript type checking passes
4. Manual spot check: `SEVERITY_EXAMPLES` string appears exactly once in prompt.ts (as a const declaration) and is referenced in both buildPrompt and buildAgenticPrompt
</verification>

<success_criteria>
- PROMPT-01: Balanced mode overlay contains 3-4 concrete anti-examples with "This is NOT a finding" framing, covering TypeScript noise (unused vars, missing return types, implicit any) and style noise (trailing newlines)
- PROMPT-03: New SEVERITY_EXAMPLES shared constant provides 4 few-shot examples (bug, security, suggestion, nitpick) with code snippets and expected finding JSON; referenced identically by both buildPrompt() and buildAgenticPrompt()
- All tests pass: prompt.test.ts (existing + new assertions), eval.test.ts (regression), npm run lint (types)
</success_criteria>

<output>
After completion, create `.planning/phases/03-prompt-foundations/03-01-SUMMARY.md`
</output>
